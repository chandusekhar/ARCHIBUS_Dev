function setLocationFieldTitle(parameters){
		parameters.geo_region_id = getMessage('geo_region_id');
		parameters.ctry_id = getMessage('ctry_id');
		parameters.regn_id = getMessage('regn_id');
		parameters.state_id = getMessage('state_id');
		parameters.city_id = getMessage('city_id');
		parameters.county_id = getMessage('county_id');
		parameters.pr_id = getMessage('pr_id');
		parameters.site_id = getMessage('site_id');
		parameters.bl_id = getMessage('bl_id');
		parameters.fl_id = getMessage('fl_id');
		parameters.rm_id = getMessage('rm_id');
		parameters.eq_id = getMessage('eq_id');
		parameters.eq_std = getMessage('eq_std');
		parameters.em_id = getMessage('em_id');
		parameters.lat = getMessage('lat');
		parameters.lon = getMessage('lon');
}